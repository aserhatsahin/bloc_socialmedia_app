name: bloc_socialmedia_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  #firebase
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1

  #BLoc
  flutter_bloc: ^9.0.0
  equatable: ^2.0.7

  uuid: ^4.4.2
  #Custom Repository
  user_repository:
    path: packages/user_repository
  post_repository:
    path: packages/post_repository

  #image
  image_picker: ^1.1.2
  image_cropper: ^9.0.0
  firebase_storage: ^12.4.4
  intl: ^0.20.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
